---
title: Aptible Customers
---
- content_for :header do
  %header.aptible-header.customers-hero
    = partial 'partials/main-nav'

    .grid-container.grid--single-center
      .grid-item.text-center
        %h2.subtitle--caps.blue Our Customers
        %h1.title.white
          You're in great company
        %p.subtitle.light-blue-gray
          We help hundreds of the most innovative companies to protect
          sensitive data when the stakes matter most.
        = partial 'partials/ctas/signup-cta', locals: { |
              class_name: 'signup-cta--centered',       |
              cta_label: 'Get Started',                 |
              cta_placeholder: 'Enter your work email'  |
            }                                           |

    .grid-container.grid--single-center.customer-page__logo-bar
      .grid-item
        .logo-bar
          - customers_for_logo_bar.each do |customer|
            .logo-bar__customer{ class: "logo-bar__customer--#{customer.slug}" }
              = partial "/images/customers/#{customer.slug}.svg"

.angled-section-dividers

.customer-page__cards.content
  .card-page
    - customer_cards_grouped_3col.each do |column|
      .card-page__column
        - column.each do |card|
          - if card[:type] == 'case_study'
            = partial 'partials/customers/case_study_card', locals: card
          - else
            = partial 'partials/customers/quote_card', locals: card


= partial 'partials/ctas/easy-as-git-push', locals: {                   |
    title: 'Get your infrastructure audit-ready in hours, not months.', |
    cta_label: 'Sign Up Now'                                            |
  }                                                                     |
