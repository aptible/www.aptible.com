<!DOCTYPE html>
<html lang="en">
  <head>
    {{> head }}
  </head>
  <!-- Intentionally slugify the page title into the body classes -->
  <body
    class="aptible-com landing-page {{#if id}}{{slugify id}}{{/if}} {{slugify title}} {{slugify page.section}} {{slugify basename}}">

    {{> body}}

    {{> footer}}

    {{> javascripts}}
  </body>
</html>
